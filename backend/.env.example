# Configuration Backend - Collecte Communautaire
# Copiez ce fichier en .env et remplissez les valeurs

# Clé secrète admin (OBLIGATOIRE - minimum 32 caractères)
# Générez une clé sécurisée avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_SECRET_KEY=

# Port du serveur (par défaut: 3003)
PORT=3003

# Origines autorisées pour CORS (séparées par des virgules)
# Exemple: https://votre-site.netlify.app
ALLOWED_ORIGINS=

# Chemin de la base de données SQLite
DB_PATH=./database/collecte.db

# Environnement (development ou production)
NODE_ENV=development
